apiVersion: v1
kind: Service
metadata:
  name: fiap-pagamentos-api-service
  namespace: fiap-pagamentos-api

spec:
  type: LoadBalancer
  selector:
    app: fiap-pagamentos-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000